import*as THREE from"three";export interface SpriteParam{texture:THREE.Texture;color?:number;transparent?:boolean;opacity?:number;}const SpriteMaterial=(a:SpriteParam):THREE.SpriteMaterial=>{return new THREE.SpriteMaterial({map:a.texture,color:a.color,transparent:a.transparent,opacity:a.opacity})};export default SpriteMaterial;