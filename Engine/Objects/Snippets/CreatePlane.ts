import*as THREE from"three";import{PositionObject3D}from"../../Constants.interface.js";export interface planeHelper{scene:THREE.Scene;size:number;color:number;}function a(b:PositionObject3D={x:0,y:0,z:0},c:number=50,d?:planeHelper):THREE.Plane{const e=new THREE.Plane(new THREE.Vector3(b.x,b.y,b.z).normalize(),c);if(d){const f=new THREE.PlaneHelper(e,d.size,d.color);d.scene.add(f)}return e}export default a;