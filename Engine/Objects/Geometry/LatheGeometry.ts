import*as THREE from"three";import{GeometryTypes}from"../../Constants.interface.js";export interface LathePhi{phiStart?:number;phiLength?:number;}export const LatheGeometry=(a:THREE.Vector2[]=[new THREE.Vector2(0,0),new THREE.Vector2(1,1)],b:number=12,c:LathePhi={phiStart:0,phiLength:Math.PI*2}):GeometryTypes=>{const{phiStart:d=0,phiLength:e=Math.PI*2}=c;return new THREE.LatheGeometry(a,b,d,e)};