import*as THREE from"three";import{GeometryTypes}from"../../Constants.interface.js";export interface SphereSegments{widthSegments?:number;heightSegments?:number;}export interface SpherePhi{phiStart?:number;phiLength?:number;}export interface SphereTheta{thetaStart?:number;thetaLength?:number;}export const SphereGeometry=(a:number=1,b:SphereSegments={widthSegments:32,heightSegments:32},c:SpherePhi={phiStart:0,phiLength:Math.PI*2},d:SphereTheta={thetaStart:0,thetaLength:Math.PI}):GeometryTypes=>{const{widthSegments:e=32,heightSegments:f=32}=b;const{phiStart:g=0,phiLength:h=Math.PI*2}=c;const{thetaStart:i=0,thetaLength:j=Math.PI}=d;return new THREE.SphereGeometry(a,e,f,g,h,i,j)};