import*as THREE from"three";import{GeometryTypes}from"../../Constants.interface.js";export interface ExtrudeOptions{depth?:number;bevelEnabled?:boolean;bevelThickness?:number;bevelSize?:number;bevelOffset?:number;bevelSegments?:number;}export const ExtrudeGeometry=(a:THREE.Shape=new THREE.Shape,b:ExtrudeOptions={depth:1,bevelEnabled:false,bevelThickness:.1,bevelSize:.1,bevelOffset:0,bevelSegments:3}):GeometryTypes=>{return new THREE.ExtrudeGeometry(a,b)};