import*as THREE from"three";import{GeometryTypes}from"../../Constants.interface.js";export interface TorusSegments{radialSegments?:number;tubularSegments?:number;}export const TorusGeometry=(a:number=1,b:number=.4,c:TorusSegments={radialSegments:16,tubularSegments:100},d:number=Math.PI*2):GeometryTypes=>{const{radialSegments:e=16,tubularSegments:f=100}=c;return new THREE.TorusGeometry(a,b,e,f,d)};