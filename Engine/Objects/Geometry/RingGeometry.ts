import*as THREE from"three";import{GeometryTypes}from"../../Constants.interface.js";export interface RingTheta{thetaStart?:number;thetaLength?:number;}export const RingGeometry=(a:number=.5,b:number=1,c:number=32,d:number=1,e:RingTheta={thetaStart:0,thetaLength:Math.PI*2}):GeometryTypes=>{const{thetaStart:f=0,thetaLength:g=Math.PI*2}=e;return new THREE.RingGeometry(a,b,c,d,f,g)};