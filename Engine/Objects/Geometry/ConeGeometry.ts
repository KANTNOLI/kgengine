import*as THREE from"three";import{GeometryTypes}from"../../Constants.interface.js";export interface ConeTheta{thetaStart?:number;thetaLength?:number;}export const ConeGeometry=(a:number=1,b:number=2,c:number=32,d:number=1,e:boolean=false,f:ConeTheta={thetaStart:0,thetaLength:Math.PI*2}):GeometryTypes=>{const{thetaStart:g=0,thetaLength:h=Math.PI*2}=f;return new THREE.ConeGeometry(a,b,c,d,e,g,h)};