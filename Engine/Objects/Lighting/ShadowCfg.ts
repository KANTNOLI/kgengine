import*as THREE from"three";import{ShadowsSize}from"./Lighting.interface.js";import{LightingTypes}from"../Constants.interface.js";export const ShadowCfg=(a:THREE.Scene,b:ShadowsSize={width:10,height:10})=>{const c=new THREE.PlaneGeometry(b.width,b.height);const d=new THREE.ShadowMaterial({opacity:.5});const e=new THREE.Mesh(c,d);e.rotation.x=-Math.PI/2;e.position.y=-.2;a.add(e);return e};