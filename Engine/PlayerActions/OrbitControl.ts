import*as THREE from"three";import{OrbitControls}from"three/addons/controls/OrbitControls.js";import{CSS3DRenderer}from"three/examples/jsm/Addons.js";export interface Angles{min:number;max:number;}export const OrbitControl=(a:THREE.WebGLRenderer|CSS3DRenderer,b:THREE.PerspectiveCamera,c:Angles={min:10,max:180}):OrbitControls=>{const d=new OrbitControls(b,a.domElement);d.enableDamping=true;d.dampingFactor=.25;d.enablePan=false;d.minPolarAngle=THREE.MathUtils.degToRad(c.min);d.maxPolarAngle=THREE.MathUtils.degToRad(c.max);return d};