import*as THREE from"three";export interface SceneVisual{backgroundColor:number;}export class CreateScene{scene=new THREE.Scene();texture=null;visual:SceneVisual={backgroundColor:0x111111};constructor(a?:SceneVisual){this.visual={...this.visual,...a};this.scene.background=new THREE.Color(this.visual.backgroundColor)}addScene(b:THREE.Object3D|THREE.Object3D[]){let c=Array.isArray(b)?b:[b];c.map(d=>{this.scene.add(d)})}}