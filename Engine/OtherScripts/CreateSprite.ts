import*as THREE from"three";import a from"./LoadingTextures.js";import b from"../Objects/Materials/SpriteMaterial.js";import{PositionObject3D}from"../Constants.interface.js";export interface SpriteParam{path:string;color?:number;transparent?:boolean;opacity?:number;}export interface Scale{x?:number;y?:number;z?:number;}const c=(d:SpriteParam={path:"your/path/to/asset"},e:PositionObject3D={x:0,y:0,z:0},f:Scale={x:0,y:0,z:0})=>{let g=new THREE.Sprite(b({texture:a(d.path),...d}));g.position.x=e.x;g.position.y=e.y;g.position.z=e.z;if(f){g.scale.x=f.x?f.x:0;g.scale.y=f.y?f.y:0;g.scale.z=f.z?f.z:0}return g};export default c;