import*as THREE from"three";export const CameraLimitSquare=(a:THREE.PerspectiveCamera,b:number=10)=>{a.position.x=Math.max(-b,Math.min(b,a.position.x));a.position.y=Math.max(-b,Math.min(b,a.position.y));a.position.z=Math.max(-b,Math.min(b,a.position.z));return a};