{
  "name": "kgengine",
  "version": "0.12.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc --emitDeclarationOnly --project tsconfig.declarations.json",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.174.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.12",
    "@types/three": "^0.174.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.8.2",
    "vite": "^5.4.1",
    "vite-plugin-node-polyfills": "^0.23.0"
  }
}
